<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Events - Football Club</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/stylesheet/style.css" />
 
</head>
<body>
    <%- include('partials/navbar') %>
    
    <div class="container my-5">
        <div class="row">
            <div class="col-12">
                <h1 class="mb-4">
                    <i class="fas fa-calendar-alt me-2"></i>Upcoming Events
                </h1>
                
                <% if (events && events.length > 0) { %>
                    <div class="row">
                        <% events.forEach(function(event) { %>
                            <div class="col-lg-6 col-xl-4 mb-4">
                                <div class="card h-100 shadow-sm">
                                    <% if (event.featured_image) { %>
                                        <img src="<%= event.featured_image %>" class="card-img-top" alt="<%= event.event_name %>" style="height: 200px; object-fit: cover;">
                                    <% } else { %>
                                        <div class="card-img-top bg-secondary d-flex align-items-center justify-content-center" style="height: 200px;">
                                            <i class="fas fa-calendar-alt fa-3x text-white"></i>
                                        </div>
                                    <% } %>
                                    
                                    <div class="card-body d-flex flex-column">
                                        <h5 class="card-title"><%= event.event_name %></h5>
                                        <p class="card-text text-muted small">
                                            <i class="fas fa-calendar me-1"></i>
                                            <%= new Date(event.event_date).toLocaleDateString() %>
                                            <% if (event.event_time) { %>
                                                at <%= event.event_time %>
                                            <% } %>
                                        </p>
                                        
                                        <% if (event.location) { %>
                                            <p class="card-text text-muted small">
                                                <i class="fas fa-map-marker-alt me-1"></i>
                                                <%= event.location %>
                                            </p>
                                        <% } %>
                                        
                                        <p class="card-text"><%= event.event_desc || 'No description available' %></p>
                                        
                                        <div class="mt-auto">
                                            <div class="d-flex justify-content-between align-items-center mb-2">
                                                <span class="fw-bold text-success">$<%= parseFloat(event.ticket_price).toFixed(2) %></span>
                                                <span class="badge bg-info"><%= event.available_tickets %> tickets left</span>
                                            </div>
                                            
                                            <% if (event.available_tickets > 0) { %>
                                                <a href="/events/<%= event.event_id %>" class="btn btn-primary w-100">
                                                    View Details & Buy Tickets
                                                </a>
                                            <% } else { %>
                                                <button class="btn btn-secondary w-100" disabled>
                                                    Sold Out
                                                </button>
                                            <% } %>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        <% }) %>
                    </div>
                <% } else { %>
                    <div class="text-center py-5">
                        <i class="fas fa-calendar-times fa-3x text-muted mb-3"></i>
                        <h3 class="text-muted">No events scheduled</h3>
                        <p class="text-muted">Check back soon for upcoming events!</p>
                    </div>
                <% } %>
            </div>
        </div>
    </div>
    
    <%- include('partials/footer') %>
   
</body>
</html>
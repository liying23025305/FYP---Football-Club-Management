<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Membership FAQs - Football Club</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
     <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/stylesheet/style.css" />
  <link rel="stylesheet" href="/stylesheet/faq.css" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
</head>
<body>
  <!-- Navbar -->
  <%- include('partials/navbar') %>
<div class="container my-5">
  <h2 class="mb-4">Frequently Asked Questions</h2>

  <!-- Category Tabs -->
  <ul class="nav nav-tabs mb-4" id="faq-category-tabs" role="tablist">
    <!-- Tabs will be rendered by JS -->
  </ul>

  <!-- Search and Category Filter -->
  <form id="faq-search-form" class="row g-3 mb-4">
    <div class="col-md-5">
      <input type="text" class="form-control" id="faq-search" placeholder="Search FAQs...">
    </div>
    <div class="col-md-3">
      <!-- Category filter dropdown -->
      <select class="form-select" id="faq-category-filter">
        <option value="">All Categories</option>
        <!-- Categories will be populated by JS -->
      </select>
    </div>
    <div class="col-md-2">
      <button type="submit" class="btn btn-primary w-100">Search</button>
    </div>
  </form>

  <!-- FAQ Accordion List -->
  <div id="faq-accordion-list">
    <!-- Accordion FAQs will be rendered here by JS -->
  </div>

  <!-- Ask a Question Form -->
  <div class="card mt-5">
    <div class="card-body">
      <h5 class="card-title">Didn't find your answer?</h5>
      <form id="faq-question-form">
        <div class="mb-3">
          <label for="faq-question" class="form-label">Your Question</label>
          <textarea class="form-control" id="faq-question" rows="3" required></textarea>
        </div>
        <div class="mb-3">
          <!-- Category select for question submission -->
          <label for="faq-question-category" class="form-label">Category (optional)</label>
          <select class="form-select" id="faq-question-category">
            <option value="">Select a category</option>
            <!-- Categories will be populated by JS -->
          </select>
        </div>
        <button type="submit" class="btn btn-success">Submit Question</button>
        <div id="faq-form-message" class="mt-2"></div>
      </form>
    </div>
  </div>
</div>
<%- include('partials/footer') %>
<!-- FAQ JS -->
<script src="/js/faq.js"></script>

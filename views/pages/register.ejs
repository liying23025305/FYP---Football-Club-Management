<style>
.register-logo {
    display: block;
    margin: 40px auto 18px auto;
    width: 90px;
}
.register-bg {
    background: #f3f3f3;
    min-height: 100vh;
}
.register-card {
    max-width: 480px;
    margin: 0 auto 60px auto;
    border-top: 4px solid #a00000;
    border-radius: 0 0 10px 10px;
    background: #fff;
    box-shadow: 0 4px 24px rgba(0,0,0,0.08);
    padding: 48px 32px 32px 32px;
    position: relative;
    text-align: center;
}
.register-title {
    font-family: 'Oswald', Arial, sans-serif;
    font-size: 2.2rem;
    font-weight: 700;
    letter-spacing: 2px;
    margin-bottom: 10px;
    text-transform: uppercase;
}
.register-subtitle {
    color: #888;
    margin-bottom: 36px;
    font-size: 1.1rem;
}
.register-email-btn {
    background: #a00000;
    color: #fff;
    border: none;
    border-radius: 25px;
    font-size: 1.1rem;
    font-weight: 600;
    padding: 14px 0;
    width: 100%;
    margin-bottom: 18px;
    transition: background 0.2s;
    letter-spacing: 1px;
}
.register-email-btn:hover {
    background: #7a0000;
}
.social-btns {
    display: flex;
    justify-content: space-between;
    gap: 10px;
    margin-bottom: 18px;
}
.social-btn {
    flex: 1;
    background: #f3f3f3;
    border: none;
    border-radius: 8px;
    padding: 10px 0;
    font-size: 1rem;
    font-weight: 500;
    color: #222;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    transition: background 0.2s;
}
.social-btn:hover {
    background: #e0e0e0;
}
.register-signin {
    text-align: center;
    color: #888;
    font-size: 1rem;
    margin-top: 18px;
}
.register-signin a {
    color: #a00000;
    font-weight: 600;
    text-decoration: underline;
}
</style>
<link href="https://fonts.googleapis.com/css?family=Oswald:700&display=swap" rel="stylesheet">
<div class="register-bg">
    <div class="register-card">
        <img src="/images/placeholder-logo.png" alt="Club Logo" class="register-logo">
        <div class="register-title">SIGN UP FOR<br>THE CLUB</div>
        <div class="register-subtitle">Use your email address or social accounts to sign in or<br>create an account</div>
        <button class="register-email-btn">EMAIL ADDRESS</button>
        <div class="social-btns">
            <button class="social-btn"><i class="fab fa-google"></i> Google</button>
            <button class="social-btn"><i class="fab fa-facebook"></i> Facebook</button>
            <button class="social-btn"><i class="fab fa-apple"></i> Apple</button>
        </div>
        <div class="register-signin">
            Already have an account? <a href="/users/login">Sign in</a>
        </div>
    </div>
</div> 
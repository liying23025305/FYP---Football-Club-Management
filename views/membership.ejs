<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Your Profile</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/stylesheet/membership.css" />
  <link rel="stylesheet" href="/stylesheet/style.css" />
</head>
<body>
  <%- include('partials/navbar') %>

  <main class="container mt-5">
    <h1 class="text-center mb-4">Your User Profile</h1>

    <div class="row align-items-center mb-4">
      <!-- Profile Picture -->
      <div class="col-md-3 text-center">
        <img src="/images/user-profile.png" alt="Profile Picture" class="img-fluid rounded-circle" style="width: 180px;">
      </div>

      <!-- Name and Tier -->
      <div class="col-md-9">
        <h2><%= user.username %></h2>
        <h5 class="text-muted">Tier: <%= user.membershipTier %></h5>
      </div>
    </div>

    <!-- Other Details -->
    <div class="row">
      <div class="col-md-6 mb-3">
        <p><strong>Email:</strong> <%= user.email %></p>
        <p><strong>Phone:</strong> <%= user.phone %></p>
      </div>
      <div class="col-md-6 mb-3">
        <p><strong>Birthday:</strong> <%= user.birthday %></p>
        <p><strong>Favorite Team:</strong> <%= user.favoriteTeam %></p>
        <p><strong>Member Since:</strong> <%= user.joinDate %></p>
      </div>
    </div>
  </main>

  <%- include('partials/footer') %>
</body>
</html>

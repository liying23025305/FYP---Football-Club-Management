<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= article.title %> - News Detail</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/stylesheet/style.css" />
  <link rel="stylesheet" href="/stylesheet/news.css" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
</head>
<body>
  <%- include('partials/navbar', { user: user }) %>
  <div class="container mt-4">
    <div class="row">
      <div class="col-md-8 mx-auto">
        <div class="card mb-4">
          <div style="background:#eee; border-radius:12px 12px 0 0; min-height:300px; overflow:hidden;">
            <img src="<%= article.featured_image %>" alt="Image" style="width: 100%; height: 100%; object-fit: cover;">
          </div>
          <div class="card-body">
            <h2 class="card-title fw-bold mb-2"><%= article.title %></h2>
            <div class="mb-2 text-muted">
              <%= article.category %> • <%= article.author_name %> • <%= article.published_at ? new Date(article.published_at).toLocaleString() : '' %>
            </div>
            <% if (user) { %>
              <button class="btn btn-sm btn-outline-warning bookmark-btn mb-3" data-id="<%= article.news_id %>" <%= isBookmarked ? 'data-bookmarked="true"' : '' %>>
                <i class="bi <%= isBookmarked ? 'bi-bookmark-fill' : 'bi-bookmark' %>"></i> Bookmark
              </button>
            <% } %>
            <div class="news-detail-content">
              <%- article.content %>
            </div>
          </div>
        </div>
        <div class="text-center my-4">
          <a href="/news" class="btn btn-outline-secondary">Back to News</a>
        </div>
      </div>
    </div>
  </div>
  <%- include('partials/footer') %>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/js/news.js"></script>
</body>
</html> 